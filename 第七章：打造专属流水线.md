# 第七章：打造专属流水线

**核心主题**：自研框架 vs 通用框架

---

## 📖 Day 7: 框架性能瓶颈

**问题诊断**：
```bash
$ ./friday --task "采燕窝" --mode autogen

Performance Report:
- Token 消耗: 15000 (异常高)
- 响应时间: 45s (超时)
- 问题: 反复确认安全协议
- 根因: 通用框架中间件层过厚
```

**系统状态**：
```
Current Framework: AutoGen
Issue: Wrapper Hell (过度封装)
Effect: 简单任务被复杂化
```

---

## 🛠️ 技术解析

### 通用框架 vs 自研框架

| 维度 | 通用框架 (LangChain/AutoGen) | 自研框架 (HelloAgents) |
|------|---------------------------|----------------------|
| **开发速度** | 快（开箱即用） | 慢（从零构建） |
| **封装层级** | 深（Wrapper Hell） | 浅（直接 API） |
| **透明度** | 低（黑盒） | **高（白盒）** |
| **灵活性** | 受框架限制 | **完全可控** |
| **调试难度** | 高（中间件多） | **低（直达核心）** |
| **适用场景** | 快速原型、通用需求 | 生产环境、定制需求 |

### 架构对比

**通用框架架构**：
```
┌─────────────────────────────────────────┐
│         AutoGen/LangChain              │
├─────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌───────┐ │
│  │ Manager  │ → │ Encoder  │ → │ LLM   │ │
│  └──────────┘  └──────────┘  └───────┘ │
│       ↓                              │
│  ┌──────────┐  ┌──────────┐  ┌───────┐ │
│  │ Validator│ → │ Parser   │ → │Output │ │
│  └──────────┘  └──────────┘  └───────┘ │
│                                         │
│  问题：10+ 中间件层 → 性能损耗、调试困难  │
└─────────────────────────────────────────┘
```

**自研框架架构**：
```
┌─────────────────────────────────────────┐
│         HelloAgents (Code-Native)      │
├─────────────────────────────────────────┤
│                                         │
│  Input → [LLM API] → Output            │
│                                         │
│  优势：                                   │
│  - 零中间件                              │
│  - 透明可控                              │
│  - 精确调优                              │
│                                         │
└─────────────────────────────────────────┘
```

---

## 💻 实现方案

### HelloAgents 核心架构

```python
# helloagents/core.py

class Agent:
    """
    最小化 Agent 实现
    设计原则: KISS (Keep It Simple, Stupid)
    """

    def __init__(self, name: str, role: str, model: str = "gpt-4"):
        self.name = name
        self.role = role
        self.model = model

        # 记忆系统
        self.memory = [
            {"role": "system", "content": f"你是{name}，职业是{role}"}
        ]

    def think(self, user_input: str) -> str:
        """
        思考 = 直接调用 LLM API（无中间商）
        """
        # 1. 存入新记忆
        self.memory.append({"role": "user", "content": user_input})

        # 2. 调用 OpenAI API（直达）
        response = client.chat.completions.create(
            model=self.model,
            messages=self.memory
        )

        # 3. 提取结果
        thought = response.choices[0].message.content

        # 4. 记住想法
        self.memory.append({"role": "assistant", "content": thought})

        return thought

    def act(self, tool_name: str, **kwargs):
        """
        行动 = 工具调用
        """
        # 工具调用逻辑
        pass
```

### 使用对比

**通用框架方式**：
```python
from autogen import AssistantAgent

agent = AssistantAgent(
    name="Friday",
    llm_config={"model": "gpt-4"}
)
# 问题：不知道内部到底做了什么
```

**自研框架方式**：
```python
from helloagents import Agent

agent = Agent(
    name="Friday",
    role="生存专家",
    model="gpt-4"
)
response = agent.think("去采燕窝")
# 优势：每一步都可控
```

### 性能测试

```bash
$ python benchmark.py

Test Case: 采燕窝任务

AutoGen:
- Tokens: 15,234
- Time: 45.2s
- Middleware Calls: 12

HelloAgents:
- Tokens: 3,456  (节省 77%)
- Time: 8.5s     (提速 5x)
- Middleware Calls: 0

Conclusion: Code-Native 完胜
```

---

## 技术决策矩阵

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| 原型验证 | 通用框架 | 快速迭代 |
| 生产环境 | **自研框架** | 性能、可控性 |
| 学习原理 | **自研框架** | 理解底层 |
| 复杂业务 | **自研框架** | 灵活定制 |

---

## 下一步计划

**当前进展**：
- ✓ 多智能体协作（第六章）
- ✓ 自研框架实现（本章）

**新问题发现**：
- 记忆容量有限
- 长对话遗忘
- 需要外接知识库

**下一步任务**：
- [ ] 第八章：RAG 系统
- [ ] 第八章：向量数据库
- [ ] 第八章：检索增强生成

---

## 系统更新

```bash
$ git log --oneline -1

c8e4d2f refactor: implement HelloAgents framework

CHANGES:
- 移除 AutoGen 依赖
- 实现 HelloAgents 核心类
- 性能优化：Token 消耗 ↓77%
- 性能优化：响应时间 ↓80%

VERSION: 0.6.0
STATUS: Friday 现在轻便灵活，但记忆有限
```

---

**→ [第八章：外接大脑](./第八章：外接大脑.md)**
