# 第二章：古老洞穴的壁画

**核心主题**：符号主义 AI vs 联结主义 AI

---

## 📖 Day 2: 发现遗留系统

**环境事件**：暴雨 → 被迫进入洞穴

**系统观察**：洞穴墙壁刻满流程图和符号

**文物分析**：
```
Type: 遗留的技术文档
Format: 流程图 + 规则表达式
Origin: 上一批"程序员"幸存者
```

**壁画内容解读**：

| 壁画 | 描述 | 技术含义 |
|------|------|---------|
| 壁画一 | IF 石头 THEN 跨过 ELSE 直走 | 符号主义规则引擎 |
| 壁画二 | 野人走得整整齐齐 | 规则驱动执行 |
| 壁画三 | 似石非石 → 野人死机 | 边界情况失败 |

**识别结果**：这是**符号主义 AI（Symbolic AI）**的遗迹。

**与 Friday 对比**：
- Friday：能用直觉处理"似石非石"（泛化能力）
- 壁画野人：遇到未定义情况直接崩溃（规则脆弱）

---

## 🛠️ 技术分析

### AI 发展史速览

```
┌─────────────────────────────────────────────────────────┐
│                    AI 演进时间轴                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1950s-1980s   1990s-2010s   2010s-2020s   2020s-Present│
│  ↓           ↓            ↓            ↓              │
│ [符号主义] → [机器学习] → [深度学习] → [LLM/Agent]     │
│                                                         │
│  Symbolic   ML/DL        Deep Learning   Generative AI  │
└─────────────────────────────────────────────────────────┘
```

### 符号主义 vs 联结主义

| 维度 | 符号主义 (Symbolic) | 联结主义 (Connectionist) |
|------|--------------------|------------------------|
| **核心思想** | 显式规则 | 概率预测 |
| **实现方式** | IF-THEN 规则 | 神经网络 |
| **知识来源** | 人工编写 | 数据训练 |
| **优势** | 可解释、精确 | 泛化能力强 |
| **劣势** | **无法处理未知** | 需要大量数据 |
| **典型案例** | 专家系统 | LLM、CNN |
| **荒岛映射** | 壁画野人 | Friday |

### 架构对比

**符号主义架构（壁画方案）**：
```python
def walk(symbolic_ai):
    if object.type == "石头":
        return action.jump_over()
    elif object.type == "泥巴":
        return action.step_on()
    else:
        return error.unknown_object()  # 💥 崩溃点
```

**联结主义架构（Friday 方案）**：
```python
def walk(connectionist_ai):
    # 神经网络基于训练数据预测下一步动作
    prediction = neural_net.predict(
        context=environment,
        next_action=space
    )
    return prediction
    # 即使遇到"似石非石"，也能泛化处理
```

### 为什么联结主义胜出？

```python
# 符号主义的致命问题
edge_cases = [
    "似石非石",   # 规则未定义
    "红色的方形石头",  # 规则未覆盖
    "会动的石头"   # 规则未预见
]
# 每个新情况都需要手动添加规则 → 无限递归

# 联结主义的解决方案
model = train(massive_dataset)
# 模型自动学习特征 → 泛化到未见情况
```

**关键洞察**：
> 现实世界是无限的，规则是有限的。用有限规则穷尽无限世界 → 不可能。

---

## 💻 设计决策

### 架构选择矩阵

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| 封闭环境（如国际象棋） | 符号主义 | 规则明确，状态有限 |
| 开放世界（如荒岛求生） | **联结主义** | 需要泛化能力 |
| 需要可解释性 | 符号主义 | 规则透明 |
| 需要处理未知 | **联结主义** | 能举一反三 |

### Friday 的技术栈分析

```python
Friday = {
    "architecture": "Transformer",
    "training_method": "Self-supervised",
    "knowledge_source": "Pre-training on massive corpus",
    "inference_mode": "Probabilistic prediction",
    "key_capability": "Generalization (泛化)"
}
```

**当前配置验证**：
- ✓ 能处理"似石非石"
- ✓ 能理解隐喻和新场景
- ✗ 但会产生幻觉（需要第三章解决）

---

## 下一步问题

**已知问题**：Friday 虽然灵活，但经常编造内容（幻觉）

**示例日志**：
```
User: "这是什么？"
Friday: "这是翡翠意面，富含微量元素。"
Reality: "这是一坨苔藓，有毒"
```

**下一步任务**：
- [ ] 第三章：理解幻觉成因
- [ ] 第三章：掌握 Prompt Engineering
- [ ] 第三章：建立约束机制

---

## 当前知识库

```bash
$ ./friday --knowledge-db

Topic                    Status
------------------------     ------
Symbolic AI               ✓ Learned
Connectionist AI          ✓ Learned
LLM Architecture          ✓ Understood
Hallucination Problem     ✗ To be addressed

Next: Prompt Engineering
```

---

**→ [第三章：沟通的艺术](./第三章：沟通的艺术.md)**
