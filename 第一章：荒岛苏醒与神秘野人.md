# 第一章：荒岛苏醒与神秘野人

**核心主题**：从 LLM 到 Agent 的架构演进

---

## 📖 Day 1: 初始环境扫描

**系统状态**：
```
Location: 未知荒岛
Resources Lost: 代码库、API 文档、开发工具
Available Asset: LLM 实体 (代号: Friday)
```

**环境描述**：海浪轰鸣，阳光刺眼。船沉了，代码库没了，这里是没有任何 API 文档的荒岛。

**发现新实体**：沙滩另一头站着一个身材魁梧的"野人"。

**实体属性分析**：
- 物理特征：肌肉发达 → 象征强大算力
- 眼神深邃 → 存储海量预训练数据
- 响应测试：我喊"你好，我是谁？"

**Friday 响应**：
```json
{
  "status": "active",
  "message": "你好！你是这片沙滩上的幸存者。根据目前的环境分析，你现在的状态属于'遇难'。"
}
```

**初步结论**：这是一个拥有人类顶级知识库的预训练模型。取名：Friday。

---

## 需求验证测试

**测试场景**：获取食物

**输入指令**：
```bash
Friday → execute("给我抓条鱼回来")
```

**Friday 响应**：
```
抓鱼是一种通过工具或徒手从水中获取鱼类资源的行为。
常见的抓鱼方法包括垂钓、网捕和叉鱼。
鱼类富含蛋白质，对你目前的生存至关重要……
```

**执行结果**：
| 时间 | 期望行为 | 实际行为 | 状态 |
|------|---------|---------|------|
| 5 min | 执行捕鱼 | 输出文本 | ✗ No Action |
| 10 min | 执行捕鱼 | 继续输出文本 | ✗ No Action |

**问题现象**：Friday 依然站在原地，口若悬河地科普《世界捕鱼发展史》，但**物理位置未发生变化**。

**交互日志**：
```
User: "动啊！去抓啊！"
Friday: "我已完成了关于'抓鱼'的文本生成。请问还需要更多关于鱼类烹饪的信息吗？"
```

---

## 🛠️ 技术诊断

### 问题分析

**观察结果**：Friday 能理解指令，但无法执行物理行动。

**诊断结论**：`Friday ≠ Agent`

**根因定位**：
```python
# 当前架构
class Friday:
    def __init__(self):
        self.brain = LLM()  # ✓
        self.tools = None   # ✗ Missing
        self.planning = None # ✗ Missing
```

### LLM vs Agent 对比

| 维度 | LLM (Chatbot) | Agent |
|------|---------------|--------|
| 输入处理 | 文本理解 | 文本理解 |
| 输出能力 | 文本生成 | **文本 + 行动执行** |
| 推理能力 | 单次响应 | **多步推理** |
| 环境交互 | 无 | **Perception → Action 循环** |

### 架构演进路径

**当前状态 (Bug)**：
```python
def friday(user_input):
    return llm.generate_text(user_input)
    # 只有文本输出，无物理行动
```

**目标状态 (Fix)**：
```python
class Agent:
    def __init__(self):
        self.llm = LLM()           # 大脑
        self.memory = []           # 记忆模块
        self.tools = []            # 工具接口

    def act(self, task):
        # 1. 感知
        perception = self.perceive()
        # 2. 规划
        plan = self.llm.plan(task, perception)
        # 3. 行动
        return self.execute_tool(plan)
```

### Agent 三要素

```
┌─────────────────────────────────────────────────┐
│                   Agent 系统                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────┐      ┌──────────────┐       │
│  │ Perception   │──────│   LLM        │       │
│  │ (感知层)      │      │   (大脑)      │       │
│  │ 看见海里有鱼  │      │   分析决策    │       │
│  └──────────────┘      └──────┬───────┘       │
│                               ↓                │
│                        ┌──────────────┐       │
│                        │  Planning    │       │
│                        │  (规划层)     │       │
│                        │ 制定抓鱼计划  │       │
│                        └──────┬───────┘       │
│                               ↓                │
│                        ┌──────────────┐       │
│                        │   Action     │       │
│                        │   (行动层)    │       │
│                        │ 执行叉鱼动作  │       │
│                        └──────────────┘       │
└─────────────────────────────────────────────────┘
```

---

## 💻 实现方案

### 代码框架

```python
# 传统的 LLM 调用（Friday 现在的状态）
def simple_chat(user_instruction):
    return llm.generate_text(user_instruction)
    # ❌ 只能返回文字，无法改变物理世界

# 目标架构
class Agent:
    def __init__(self, name="Friday"):
        self.llm = LLM()          # 推理引擎
        self.memory = []          # 记忆缓冲区
        self.tools = []           # 工具注册表

    def act(self, task):
        # Step 1: 任务分解
        plan = self.llm.think(task)

        # Step 2: 工具调用决策
        if self.need_tool(plan):
            tool = self.select_tool(plan)
            return tool.execute()
        else:
            return self.llm.speak(plan)

    def register_tool(self, tool):
        self.tools.append(tool)
```

### 下一步行动计划

- [ ] 第二章：理解神经网络原理（符号主义 vs 联结主义）
- [ ] 第三章：掌握 Prompt Engineering（控制幻觉）
- [ ] 第四章：实现 ReAct 循环（观察-思考-行动）

---

## 当前系统状态

```bash
$ ./friday --status

Component           Status
----------------     ------
LLM Engine            ✓ Online
Memory Module         ✗ Not Implemented
Tool Interface        ✗ Not Implemented
Planning System       ✗ Not Implemented

Overall: CHATBOT MODE (Action Disabled)
```

**备注**：太阳快落山，Friday 仍在输出文本。需要尽快升级到 Agent 架构。

---

**→ [第二章：古老洞穴的壁画](./第二章：古老洞穴的壁画.md)**
